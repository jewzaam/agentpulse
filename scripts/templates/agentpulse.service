[Unit]
Description=AgentPulse — Claude Code agent state service
After=network.target

[Service]
Type=simple
ExecStart=%h/.local/bin/agentpulse --config %h/.claude/agentpulse/config.json
Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
