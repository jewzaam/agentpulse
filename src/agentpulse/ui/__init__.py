# Generated By: Claude Code (Claude Opus 4.7 (1M context))
"""Optional dashboard UI for AgentPulse.

This module is intentionally self-contained — its only touchpoint with
the rest of the service is a single ``app.include_router(ui_router)``
call in ``app.py``. Removing that line and deleting this directory
cleanly removes the feature.
"""

from agentpulse.ui.router import router

__all__ = ["router"]
