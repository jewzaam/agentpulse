# Generated By: Claude Code (Claude Opus 4.7 (1M context))
"""Windows-specific helpers shared between the tray supervisor and the
autostart installers."""

import sys
from pathlib import Path


def pythonw_path() -> str:
    """Return pythonw.exe sibling to sys.executable when available.

    pythonw.exe is the Windows console-less Python interpreter. Using it
    for long-running background processes avoids attaching a console
    window at launch — critical for autostart shortcuts that fire at
    login, and for the tray app's service-restart action. Falls back
    to sys.executable when pythonw.exe is not present (e.g. embedded
    Python distributions).
    """
    exe = Path(sys.executable)
    pythonw = exe.with_name("pythonw.exe")
    if pythonw.exists():
        return str(pythonw)
    return sys.executable
