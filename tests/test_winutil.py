# Generated By: Claude Code (Claude Opus 4.7 (1M context))
"""Tests for agentpulse.winutil — pythonw.exe resolution."""

from pathlib import Path

import pytest

from agentpulse import winutil


class TestPythonwPath:
    def test_prefers_pythonw_when_present(
        self, tmp_path: Path, monkeypatch: pytest.MonkeyPatch
    ) -> None:
        fake_exe = tmp_path / "python.exe"
        fake_exe.write_text("")
        fake_pythonw = tmp_path / "pythonw.exe"
        fake_pythonw.write_text("")
        monkeypatch.setattr("sys.executable", str(fake_exe))

        assert winutil.pythonw_path() == str(fake_pythonw)

    def test_falls_back_when_pythonw_missing(
        self, tmp_path: Path, monkeypatch: pytest.MonkeyPatch
    ) -> None:
        fake_exe = tmp_path / "python.exe"
        fake_exe.write_text("")
        monkeypatch.setattr("sys.executable", str(fake_exe))

        assert winutil.pythonw_path() == str(fake_exe)
