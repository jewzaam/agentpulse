# Generated By: Claude Code (Claude Opus 4.7)
"""Conftest for install-script tests.

- Adds the repo's ``scripts/`` directory to sys.path so the installers
  can be imported as plain modules.
- Overrides the root conftest's subprocess block (each test mocks
  subprocess.run directly).
"""

import sys
from pathlib import Path
from typing import Iterator

import pytest

_SCRIPTS_DIR = Path(__file__).resolve().parent.parent.parent / "scripts"
if str(_SCRIPTS_DIR) not in sys.path:
    sys.path.insert(0, str(_SCRIPTS_DIR))


@pytest.fixture(autouse=True)
def _block_subprocess() -> Iterator[None]:
    """No-op: install-script tests mock subprocess.run directly."""
    yield
